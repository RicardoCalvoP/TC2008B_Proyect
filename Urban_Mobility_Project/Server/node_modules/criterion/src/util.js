// Generated by CoffeeScript 1.6.3
module.exports.beget = function(proto, properties) {
  var k, object, v, _fn;
  object = Object.create(proto);
  if (properties != null) {
    _fn = function(k, v) {
      return object[k] = v;
    };
    for (k in properties) {
      v = properties[k];
      _fn(k, v);
    }
  }
  return object;
};

module.exports.arrayify = function(thing) {
  var array, k, v, _fn;
  if (Array.isArray(thing)) {
    return thing;
  }
  array = [];
  _fn = function(k, v) {
    var obj;
    obj = {};
    obj[k] = v;
    return array.push(obj);
  };
  for (k in thing) {
    v = thing[k];
    _fn(k, v);
  }
  return array;
};

module.exports.isRaw = function(value) {
  return (value != null) && 'function' === typeof value.sql;
};
